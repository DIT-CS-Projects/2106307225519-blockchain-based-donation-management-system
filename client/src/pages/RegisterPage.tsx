import { PagePlaceholder } from '@/components/shared/PagePlaceholder'

export function RegisterPage() {
  return (
    <PagePlaceholder
      eyebrow="Account"
      title="Create an account"
      description="Registration ships in Stage 3. Donations will not require an account."
    />
  )
}
