import { PagePlaceholder } from '@/components/shared/PagePlaceholder'

export function AboutPage() {
  return (
    <PagePlaceholder
      eyebrow="About"
      title="Why Tuma exists"
      description="Tuma makes charitable giving in Tanzania verifiable — familiar payments in front, an immutable record behind. The full story lands here soon."
    />
  )
}
