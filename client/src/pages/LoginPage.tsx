import { PagePlaceholder } from '@/components/shared/PagePlaceholder'

export function LoginPage() {
  return (
    <PagePlaceholder
      eyebrow="Account"
      title="Log in"
      description="Authentication ships in Stage 3. Donations will not require an account."
    />
  )
}
